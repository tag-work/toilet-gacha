# トイレいったら ガチャ — プロジェクトメモ

3歳からのトイレトレーニング応援アプリ。作者は tagc、最初のユーザーは3歳の息子。
本番: https://tagc.works/toilet-gacha/

姉妹アプリ「えいご シールクイズ」「たしざんクイズ」と同じ設計思想・同じキャラクター。
共通ルールは親ディレクトリの CLAUDE.md を参照。

## 中核の設計思想（変えないこと）

**「成功」ではなく「トイレに行ったこと」でガチャが回る。**
出るかどうかは子どもにコントロールできない。コントロールできる行動を数える。

**3つのガチャ（でなかった／おしっこ／うんち）は当たりやすさが同じ。**
差をつけると「でなかった」を選ぶことが罰になり、正直に記録されなくなる。

**失敗を記録する機能は作らない。** for-parents に明記済み。

## ファイル

`index.html` に全部入り。ビルドツールなし。外部依存は Google Fonts のみ。

## 実装上の注意

- **SVGのグラデーションIDは描画ごとにユニークにする。** `drawCar()` の `rb0,rb1...`、
  `machineSVG()` の `gl0,gl1...`。固定IDにすると、同一IDが複数存在したときに
  ブラウザが最初の1つを参照し、非表示要素を拾って色が変わる（実際に発生した）
- 車24種は `CARS` 配列。追加は1行足すだけ。`shape` と `top` の組み合わせで描き分ける
- 排出率はノーマル70%／レア25%／SR5%。全24種コンプまで平均127回・中央値111回。
  1日5回で3〜4週間の想定
- パスは相対（`./`）。`index.html` を変えたら `sw.js` の `const V` を上げる

## 広告

**アプリ本体（index.html）には広告を入れない。** for-parents と toitore/ 配下のみ。
子ども向けなので `data-tag-for-child-directed-treatment="1"` を付ける。

## コンテンツ

`toitore/` の3記事は実体験ベース。医学的な断定はしない。
個人差があること・気になるときは小児科に相談することを必ず添える。
